# Set this only for deployed builds (for example, in Vercel environment variables).
# It must be the public HTTPS origin of your Express API, without /api at the end.
VITE_API_URL=https://your-backend.example.com
